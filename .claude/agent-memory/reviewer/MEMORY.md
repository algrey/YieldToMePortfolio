# Memory index

- [Recurring review issues](review-recurring-issues.md) — parser-accepts-the-"malformed"-fixture tests, guards added on the wrong layer, advisory warnings that fire on every re-synced row, envelope mismatches, source-regex tests, alias-blind EXPLAIN guards, OR-window index degradation, D1 100-param cap, orphan promises, missing ARCHITECTURE.md entries, route-redirect blast radius, `String(null)` sentinels, unverified "owner's symptom" causation, cron-tick daily `rows_read`, bound-vs-chunk-size durability claims, one cursor shared by two streams, `status='posted'` reversal mirrors, throwing decimal parsers, scope-narrowing on invariants that don't hold, mirrored-query tests, IS NULL predicates hijacking the index, cache-side-only fingerprints, rows-returned "after" costs, classifiers comparing fewer fields than the digest, cause-from-`reused` inferences, "all tests fail pre-fix" claims, loop-built INSERTs growing an atomic batch past its own declared statement cap, fixes that relieve one *_LIMITS constant and break its sibling, mirrored post-action advances that drop the sibling route's terminal-status gate, gate fixes that narrow WHICH portfolios get advanced, statement-vs-subrequest budget accounting, "nothing deletes from this table" claims disproved by the dynamic-name account-purge path, status subqueries that omit the SUCCESS status and so flag a stale failure forever, the accepted "newer than the published run" remedy plus the gains loader's zero-sell short-circuit, new test files that break `tsc --noEmit` while every runtime check stays green, and typed-error-string renames that strand the page's error→copy mapper plus its normative doc, the accepted restore-the-contract + real-mapper-via-tsx-child-process remedy, and `safeX` wrappers that are blind to the input their wrapped function returns verbatim (the throw moves to the comparison, and to the commit route), and the accepted atomic statement-builder-seam remedy for unbatched create-then-link orphans (plus the trigger-based mid-batch rollback probe), and gate-made-conditional diffs that correct only the new decision-log entry while MARKET_DATA_STRATEGY/CALCULATIONS still describe the gate as unconditional, plus "N of M new tests fail pre-fix" counts inflated by guards that pass an added optional parameter, fail-open caps on a set with a second (older) consumer, unary-plus index hints that no test pins, cached-negative-fact markers whose clear list only covers paths that already invalidated the cache (Sharesight price accretion and FX writes never did), shared statement builders whose doubled output breaks a caller's runtime-enforced fail-closed batch cap, preview-only self-validating wrappers whose commit-side twin still forwards the input verbatim (and then PERSISTS it, against the diff's own "nothing is written" claim), cherry-pick "kept both notes" duplicate doc paragraphs that preserve the sentence the round was dispatched to correct, cap REMOVALS that leave the imp-004a `pagePreview` mirror still capping under a parity comment, evidence paragraphs crediting the wrong test file for each new guard, and the accepted one-bound-at-the-write-boundary remedy (DECIMAL_LIMITS-referenced, fuzz-verified parity) whose sibling owner-typed writer and total-digits clause stay uncovered, plus the accepted fidelity-round remedy for both (byte-identical mirror restore in EVERY mirror file + dated in-place "(corrected ... after review: ...)" notes), plus invalidation fixes that re-open the same hole through a shared `LIMIT maxPortfolios` builder spending a cross-owner budget on a user-scoped write, and unique-index NARROWINGS whose newly-reachable data shape the bundle/system-restore replay (`ledger.post` + Kahn `chainOrder`) can no longer reconstruct, plus the accepted optional-trailing-filter + `rawDetail` + `identityConfirmed` template for narrowing a shared whole-portfolio loader (PRF-013), and per-portfolio `[MIN,MAX]` invalidation ranges whose span a stale provider market date blows open. Also: ordering-function changes that break a count-based chunked-restore resume across deploys, and the accepted exact-date `value_date IN (…)` remedy for a data-controlled invalidation span (54d, fuzz-verified, with its untested bound-parameter guard). Also: extraction commits that leave prior ARCHITECTURE.md file attributions (and a second suite's hand-typed copy) pointing at the old file — plus the accepted pure-query-builder extraction template and its accepted correction round (dated in-place §9.7 markers, test copies re-pointed at the builders), and the accepted all-writers dividend decimal-bound remedy (BUG-022): the three-INSERT writer table, the 1,260-value parity fuzz, and the unreachable franking false-positive shape. Also: shared "resolved context" supersets whose identity read INNER JOINs `securities`, dropping `unresolved` rows and flipping which loader cap fires (56). Also: read-time null-the-corrupt-value isolation feeding a downstream absent-means-zero inference (fabricated $0 franking, 57). Also: ordering fixes that close one of two symmetric resume phases and then retire the operational rule for both (plus a re-introduced `git stash` provenance claim the same doc already corrected, 58), and finalize-time existence checks that verify the client's own ref list (59). Also: null-fallback paths whose OUTPUT parity is real but whose degraded-path census exceeds the pre-feature baseline, and the genuinely mixed emitStructuredLog-vs-console.warn convention (60/60b). Also: partial-index narrowings that free one terminal status while the task's "decide the sibling terminal status" deliverable goes unrecorded (62). Also: never-observed-shape guards whose one PRESENCE-based trigger (`next_page`) fires on the provider's own "no next page" encoding plus a bare top-level `total` that reads as money, and the spike header claiming no ids are printed while its metadata-value printer emits `links.self` (63/63b). Also: EXPLAIN pins built on a hand-`CREATE TABLE`d (index-free) fixture that record the opposite of the migrated-schema plan, and source-scan "a future writer must fail this test" guards blind to the repository's own bound-parameter mutator (64/65). Also: doc rewords applied to the trigger paragraph while the same section's `Tests:` inventory still lists the retired trigger (63c, with the reusable BRK-017 matrix + redaction recipe). Also: retired-protocol rounds whose production module header still describes the retired mechanism as current and safe (58b), plus the accepted scaffold-persisted ref-digest remedy for client-supplied expected sets and its NULL-digest fail-open residual (59b). Also: bundle-slimming diffs whose dead-fallback + byte-identical-render claims hold but whose pin shrinks to one source file while the scope asked for a build-output pin (61, plus a third `git stash` provenance sentence), and its accepted remedy: a duplicated bug-001 `"use client"` walker + a security-headers-shaped `dist/client` absence grep, with dated "read it as" doc notes (61b). Also: the accepted verbatim `.tsx`->`.ts` pure-block extraction template (61c: export-stripped difflib check, pins mutation-tested in their NEW home + a surviving usage-site pin, 9-section render diff, two-tree chunk bytes) and the session-scratchpad sibling-agent collision that fakes `format:check` failures.  Also: value comparators that COMPUTE the incoming side but read the committed side raw, so the per-share storage mode the comment claims to support is the one that false-positives (66), and computed-only "blocking" preview issues that never reach the Sharesight Accept gate (persisted-issues-only) nor the ready-service's own standalone loader, leaving a paid-date correction to double-write (67). Also: the accepted per-field `unreadableFields` marker remedy for read-time sanitizers feeding absence inferences, whose unread `frankingPerShare` marker lets a default assumption silently stand in for a corrupt stored value and count as KNOWN franking (57b, re-verified 57c), and the round that finally consumes such a marker changing a documented calculation chain with no CALCULATIONS.md paragraph (57d). Also: observe-then-withdraw sync passes whose observed set is the classifier's current bucket (a date-threshold crossing withdraws the row) and "pending and committed share ONE key" claims where the committed side is the `import-fingerprint:`-prefixed derivative (70/71), plus their accepted remedies -- whole-fetch observed set + a shared committed-key minter -- and that round's residuals: the "single place that mints" over-claim, the CSV-fingerprint suppression hole, and the false "nowhere to persist this disclosure" excuse that row-less `import_issues` entries disprove (70b/71b/72). Also: paid-only/disclosure fixes applied to a component's primary render path while its degraded fallback mapper still renders the raw figure, and "identical predicate as X" claims that X's own doc comment contradicts (73/74), plus that round's correction: swapping ONE cell to a paid-only figure while the row's yield cell and detail-dialog cash/franking breakdown still derive from the gross (75), and a tightened source-regex pin justified by a false claim about what the OLD regex matched (76). Also: the accepted full-FY-row + received-so-far-slot remedy for that, whose reverted "now PAID-only" claim survived in the JSX render comment (75b), plus the sandbox `npx tsx` EPERM workaround.
- [Sharesight sync invariants](project-sharesight-sync-invariants.md) — GET-only seal location, staging-vs-commit watermark hazard, `source_reference` key shapes, digest value-bearing field list, identity-only commit dedupe, reversal filters, real query-param names, per-isolate token memo + Promise.all stream fetch.
- [Derived-state dependency map](project-derived-state-dependencies.md) — which tables each derived cache/publication actually consumes; use before judging any invalidation-widening diff.
- [Verification commands](review-verification-commands.md) — what to run, schema-only EXPLAIN probes, old-vs-new loader equivalence recipes, depth census (incl. disabling DEPTH_CEILING in the pre-fix copy), whole-commit pre-fix trees, and the one pre-existing lint warning that is not a finding.
- [Free-plan constraints on backup/restore](project-free-plan-constraints.md) — why EXP-003 exists: Workers Free CPU/D1 limits shape the backup transfer design.
- [Snapshot pipeline retired (CALC-005)](project-snapshot-pipeline-retired.md) — what is now dead code, what must never be re-queued, and the CALC-005 ID collision.
- [Chunked restore invariants](project-backup-restore-design.md) — derived keys, resume-in-place, fresh-account precondition; what must not break in later backup work.
