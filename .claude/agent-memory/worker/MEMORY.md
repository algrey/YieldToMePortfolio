# Memory index

- [Test-runner import constraints](test-runner-constraints.md) — plain Node test runner can't import `.tsx` files or anything pulling in `next/headers`; extract pure logic to a `.ts` sibling instead.
- [Node test-file import side effect](node-test-file-import-side-effect.md) — importing another `*.test.ts` file's helper re-executes its top-level `test()` calls too; duplicate small walker/parser logic locally instead.
- [Resumable restore design](resumable-restore-design.md) — chunking a monolithic D1 commit across requests: durable derived keys beat in-process maps, archive-and-recreate retry strategies must become resume-in-place, prefer server-derived resume counts over client cursors, audit "finalize" steps for latent non-idempotency.
- [D1 query planning](d1-query-planning.md) — correlated EXISTS defeats indexes; full-row-fetch for a yes/no question wastes CPU even when seekable; count(*) precheck redundancy; duplicate identity resolution doubles a write; multi-window OR'd BETWEEN pattern for N narrow date ranges; fixture FK gotchas.
- [Worker TASKS.md boundary](worker-tasks-md-boundary.md) — never edit TASKS.md as a Worker even when the delegated task explicitly says to; that's Orchestrator-only per AGENTS.md's role division.
- [Sequential-depth parallelization](sequential-depth-parallelization.md) — D1 round-trip DEPTH (not call count) is a separate cost axis on Workers Free; how to measure it and the recurring safe-to-parallelize/must-stay-sequential shapes in this repo's loaders.
- [wrangler.json strict format](wrangler-json-strict-format.md) — keep it strict JSON, no comments; tests JSON.parse it directly even though wrangler itself tolerates JSONC.
- [vinext Link prefetch](vinext-link-prefetch.md) — auto-prefetch skips only dynamic PATH segments, not force-dynamic pages; a fixed-path link like "/" that's always in-viewport gets silently re-prefetched on every navigation.
- [HIST-001 no-batch invariant](hist-001-no-batch-invariant.md) — app/historical-portfolio-value.ts is pinned to never call client.batch()/client.run(); blocks a batch()-based rows_read fix for /income.
- [PRF census batch harness gap](prf-census-batch-harness-gap.md) — stageCensusClient missed client.batch() statements in stats.calls_ (fixed PRF-006); check before trusting a census that used batch().
- [vinext redirect vs try/catch](vinext-redirect-vs-trycatch.md) — redirect() throws a digest-tagged error vinext doesn't auto-rethrow; a wrapping try/catch silently swallows it. Use an output-slot + caller-side redirect() instead.
- [portfolio-shell owned-mode narrowing](portfolio-shell-owned-mode-narrowing.md) — `ownedMode && ownedWorkspace.foo` type-checks via TS aliased-condition narrowing; match this idiom, don't add `?.`/`!`.
- [import-commit polymorphic FK](import-commit-polymorphic-fk.md) — `import_rows.commit_transaction_id` points at `transactions.id` OR `dividend_manual_records.id` with no discriminator; single-table JOIN drops rows (PRF-007); UNION-across-kinds traps: NULL-turns-truthy-"null" via `String()`, and a combined MIN/MAX range isn't right for every consumer.
- [Sharesight sync window investigation](sharesight-sync-window-investigation.md) — real API param names are `start_date`/`end_date` not `from`/`to` (BRK-015 root cause); payout identity key excludes the Sharesight `id`; sandbox network notes for live Sharesight spikes.
- [Import reconciliation advisory-warning pattern](import-reconciliation-advisory-warning-pattern.md) — the 6-step recipe for a non-blocking "possible duplicate" preview warning (BUG-011/DIV-004): warning severity, caller-supplied existing-entries field, per-row loop guard, MUST exclude from previewVersion hash, decimal-string comparison, no UI change needed.
- [Value-history convergence marker pattern](value-history-convergence-marker-pattern.md) — PRF-010: a "missing = A minus B" fingerprint must probe BOTH tables, not just the cache side; aggregate .get() calls need their own count field, not "1 row returned"; new-table vs new-column cost tradeoff.
- [Sharesight already-imported classification](sharesight-already-imported-classification.md) — BRK-014: field must match digest set (B1) AND have a faithful committed counterpart, not just be digest-hashed (round 3) -- else it's residual.
- [PRF-007 parity-queueing precedent](prf-007-parity-queueing-precedent.md) — "mirror X's queueing for parity" overrides "nothing consumes this data" when X's own task already established parity as the deliverable (BUG-016 vs PRF-007).
- [calculation_runs tiebreak flakiness](calculation-runs-tiebreak-flakiness.md) — supersedeStaleQueuedRuns/nextClaimable/hasNewerRun UUID-tie flake: FIXED in BUG-020 via rowid tie-break; no longer needs a test delay workaround.
- [calculation_runs false "no delete path" claim](calculation-runs-no-delete-path-false-claim.md) — before writing "table X's rows are never deleted", check account-lifecycle.ts's OWNED_TABLES/PURGE_TABLES_IN_FK_ORDER and drizzle purge-lock triggers first.
- [Source-shape pins and contract types](source-shape-pins-and-contract-types.md) — BUG-017: some tests regex-pin a loader's literal source shape (use an output-slot to add a field without breaking them); presentation types live in a `*-contract.ts` sibling by convention, not the loader file.
- [BUG-017 newer-than-published-run pattern](bug-017-newer-than-published-run-pattern.md) — a "latest matching-status row" subquery must compare against the row currently being served, not just filter candidates by status, or a stale terminal state masks a later success forever.
- [Redaction key pattern gotcha](redaction-key-pattern-gotcha.md) — `SENSITIVE_KEY` auto-redacts any metadata key matching `user.?id`/`portfolio.?id`/`security.?id`/`target.?id` before it reaches a log sink; expected, not a bug to route around.
- [BRK-016 narrowing and overclaim pattern](brk-016-narrowing-and-overclaim-pattern.md) — `T | null = null` set only inside a Promise executor doesn't narrow after `assert.ok`; use `T | undefined` with no initializer. Plus the doc-overclaim shapes a concurrency-fix review round catches.
- [Count-gate removal vs orphan detection](count-gate-removal-vs-orphan-detection.md) — a "redundant" count(*) can also police JOIN-completeness, not just overflow; removing both needs LEFT JOIN + per-field validation, not a plain delete (PRF-011).
- [PRF-011 projection_publications PK](prf-011-projection-publications-pk.md) — `portfolio_id` is that table's PRIMARY KEY, so a LIMIT-2 multiplicity check's "2+ rows" branch is schema-unreachable; don't build a test for it.
- [PRF-002 census harness extension cost](prf-002-census-harness-extension-cost.md) — adding a page to PAGES/DEPTH_CEILING means re-deriving its full next/headers-free read sequence; prefer a source-regex + oneResolutionStatements test when the fix is just "one fewer identity resolution".
- [Sandbox /tmp write denied](sandbox-tmp-write-denied.md) — heredoc/tsx writes to /tmp silently fail or EPERM under this sandbox; use the session scratchpad dir and plain `node` for throwaway measurement scripts.
- [Edit tool unicode dash mismatch](edit-tool-unicode-dash-mismatch.md) — Edit's old_string fails on em-dash/arrow prose copied from Read output; use a python3 heredoc str.replace instead.
- [BRK-022 slice 3 read-path patterns](brk-022-slice3-read-path-patterns.md) — second-flag tier discriminator (not repurposed field); census-pin +1 whenever loadOwnedDividendHistory gains a read; loosen (don't delete) source-shape regex pins; `?? null` not `=== null` for new optional component fields; build income-multi-year fixtures via the real projectMultiYearIncome, never hand-JSON.
